let typed = new Typed("#type", {
    strings: ["Eav Outdom", "21 year old", "from Kompong Cham"],
    typeSpeed: 100,
    backSpeed: 100,
    loop: true,
});

$('.timeline').timeline({
    forceVerticalMode: 800,
    mode: 'horizontal',
    visibleItems: 4
  });